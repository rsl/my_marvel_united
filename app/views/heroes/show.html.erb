<h2><%= @hero.name %></h2>

<%= image_tag @hero.card.variant(resize_to_fit: [300, 300]), class: 'card' %>

<table class="table foo">
  <tr>
    <th scope="row">Box</th>
    <td><%= link_to @hero.box.name, box_path(@hero.box) %></td>
  </tr>
</table>
